services:
  - type: web
    name: vita-gaurd-cloud-api
    env: python
    plan: free
    buildCommand: "python -m pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
